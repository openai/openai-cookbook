# Generated outputs

GitHub Actions writes generated PNG files and `manifest.json` into this directory inside the workflow workspace, then publishes the directory as a temporary review artifact.

Generated files are ignored by Git and are not published automatically.
