# Docker sandbox

[Run the application-managed example](application_managed/README.md) to build a
local executor image, run a task, and clean up the container and API session.

This example requires Docker. It does not include a webhook controller.
