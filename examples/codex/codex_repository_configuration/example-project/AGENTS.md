# Repository guide

- Run `npm test` after changing JavaScript.
- Keep public functions in `src/` and their tests in `test/`.
- Use Node.js built-in modules unless the task requires a dependency.
- Do not commit `node_modules/`, coverage output, or generated files.
- Do not commit or push unless the user asks.
