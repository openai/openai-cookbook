# Prior authorization AgentCore support code

The adjacent Cookbook notebook imports this package to keep the walkthrough concise. The `runtime_source` directory contains the complete Python application and package manifest that the notebook validates, packages, and deploys to Amazon Bedrock AgentCore Runtime.
