# AUMARA Nominalia domain routing probe

- Previous probe contained an unsafe credential-bearing FTP diagnostic string and has been redacted from the current tree.
- A hardened probe now records only DNS, HTTP status, vhost routing and a boolean FTP access check.
- See the latest successful `Probe AUMARA Nominalia domain routing` workflow output for current routing evidence.
