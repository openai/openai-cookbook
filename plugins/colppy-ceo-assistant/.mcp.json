{
  "mcpServers": {},
  "_comment": "No remote MCP servers needed. Intercom access uses Claude Desktop's built-in Intercom integration (Settings → Integrations → Intercom). No tokens or local server config required."
}
